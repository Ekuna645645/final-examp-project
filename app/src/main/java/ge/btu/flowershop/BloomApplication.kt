package ge.btu.flowershop

import android.app.Application

/**
 * Application entry point. Firebase auto-initializes from google-services.json via its
 * content provider, so there's nothing to do here yet (FCM/notifications come later).
 */
class BloomApplication : Application()
